spring:
  application:
    name: organizationservice
  profiles:
    active: default
  cloud:
    nacos:
      config:
        server-addr: localhost:8848
        file-extension: yaml
encrypt:
  key: secret